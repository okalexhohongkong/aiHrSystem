# 黑卫士 AI HR 系统 V1.2 单机可测版

这是黑卫士 AI HR 系统的 Web/PWA 单机可测版本，当前目标是用于本机安装、页面演示、业务流程确认和核心本机数据保存验证。它不是最终生产版，真实数据库、真实短信/邮件/微信发送、真实招聘平台接口、真实 ASR/AI 接口会在后续联调版继续接入。

## 当前可测范围

- 总览看板：候选人总数、推荐复试、录音解析、待提交作业、岗位滚动窗口。
- 候选看板：候选人长条列表、投递时间排序、匹配度、资料完整度、同行权重、技能标签。
- 岗位发布：AI 辅助 JD、岗位类型、岗位动态数据、标杆公司画像。
- 招聘账号：多招号招聘矩阵、登录方式预留、招聘平台适配器预留。
- 邀约预约：邮件、电话、短信、微信、企微、平台留言预约流程。
- 录音解析：录音上传、会议接入、转写纪要、面试档案分析的前端流程。
- 初试评分：九维模型、可拖拽维度、蜘蛛网和长条评分表达。
- 岗位问卷：岗位问卷卡片库、题库、加分项、减分项、AI 反向生成问卷。
- AI 邀约面试：文字/语音 AI 面试流程、佐证材料要求。
- 面试编排：在线、初试、复试、终试的轮次安排。
- 三轮作业：作业题、出题人、考官、答卷、评分、案例归档。
- 自动分工：自动分配人事/面试官、双向确认、超时报警。
- 端口 Agent：Agent Skills、APP/PWA 下载通道、短信/邮件/二维码/链接发送入口。
- 简历库、数据看板、成果目标、合约模块、反向求职调研、合伙兼职、移动工作端、平台底座、多端路线、风险合规。
- 界面皮肤：企业 Logo/名称、颜色调色盘、字体、字号、左侧菜单宽度/字号/行距、母菜单显示隐藏。
- 系统后台：账号、权限、版本等级、招聘账号连接管理。演示权限码：`123456`。

## 当前已启用的本机保存

- 系统后台：本机数据备份、导入恢复、清空本机数据。
- 候选看板：候选人状态流转、备注、操作记录保存。
- 岗位发布：岗位草稿字段编辑保存，包括岗位名称、英文名称、岗位缩写、部门、目标人数、JD 状态。
- 邀约预约：邀约队列新增、状态更新和统计保存。
- 初试评分：推荐复试动作会写入候选人操作记录。
- 三轮作业：三轮作业布置、提交状态和候选人留痕保存。
- 简历库：转储备、归档淘汰动作会写入候选人操作记录。

## 当前边界

- 当前版本使用浏览器本机存储，适合单机测试和演示，不等同于多人协同数据库。
- 真实 BOSS 直聘、短信、邮件、微信、企微、会议系统、ASR 和 AI 模型接口尚未接入生产账号。
- 外部平台自动化只能在授权账号和平台规则允许范围内做合规接入；当前包先跑通本机流程、数据沉淀和人工确认链路。

## 从 GitHub 下载后启动

推荐使用固定依赖安装命令，确保别人下载后界面和当前版本一致：

```bash
git clone git@github.com:okalexhohongkong/aiHrSystem.git
cd aiHrSystem
npm ci
npm run demo
```

然后打开：

```text
http://127.0.0.1:5173/
```

如果已经安装过依赖，以后只需要在项目目录执行：

```bash
npm run demo
```

也可以双击项目根目录的 `start-demo-mac.command` 启动。

## 仓库内整站文件夹

仓库已经包含一份可直接部署的整站静态文件：

```text
whole-site/黑卫士AIHR-整站文件夹/
```

如果只想查看整站文件，不改代码，可以进入该目录，用静态服务器打开：

```bash
python3 -m http.server 8080 --bind 127.0.0.1 --directory "whole-site/黑卫士AIHR-整站文件夹"
```

然后访问：

```text
http://127.0.0.1:8080/
```

## Mac 本机演示启动

```bash
npm ci
npm run demo
```

## Windows 本机演示启动

在项目目录双击 `start-demo-windows.bat`，或在命令行执行：

```bat
npm ci
npm run demo
```

浏览器打开：

```text
http://127.0.0.1:5173/
```

## 生产演示包

生成静态演示包：

```bash
npm run build
```

构建产物在：

```text
dist/
```

本机预览生产包：

```bash
npm run demo:prod
```

## Mac 单机 App 打包

生成经典版和国际版两个 Mac 单机演示包：

```bash
./scripts/package-mac-demo.sh all
```

生成后的位置：

```text
release/黑卫士 AI HR 经典版-mac.dmg
release/黑卫士 AI HR 经典版-mac.zip
release/黑卫士 AI HR 国际版-mac.dmg
release/黑卫士 AI HR 国际版-mac.zip
```

说明：

- 经典版是 A 计划主线，不显式展示国际 HR 参考库。
- 国际版是 B 计划备用，显式展示国际 HR 参考库。
- `release/` 和 `dist/` 是本机生成产物，不提交到 Git；别人克隆后用上面的命令重新生成即可。

## 安装到 Mac 桌面

当前版本是 Web/PWA，可通过浏览器安装到桌面：

1. 先启动 `npm run demo` 或 `npm run demo:prod`。
2. 用 Chrome 或 Edge 打开 `http://127.0.0.1:5173/`。
3. 点击地址栏右侧的“安装应用”图标。
4. 安装后可以像普通桌面应用一样从启动台打开。

Safari 可使用“文件 / 添加到程序坞”方式安装为桌面入口。

## 演示检查顺序

1. 打开总览，确认岗位与面试者滚动窗口正常显示。
2. 点候选看板，确认长条候选人列表、排序按钮、匹配度和标签可见。
3. 在候选看板修改候选人状态并填写备注，刷新页面后确认状态和操作记录还在。
4. 点岗位发布，编辑岗位草稿字段，刷新页面后确认内容还在。
5. 点邀约预约，新增一条邀约记录并修改状态，刷新页面后确认队列还在。
6. 点三轮作业，布置作业或修改提交状态，回到候选看板确认候选人留痕。
7. 点界面皮肤，测试企业 Logo、颜色、字体和字号设置。
8. 点系统后台，输入演示权限码 `123456`，测试本机备份、恢复、清空入口。
9. 点端口 Agent，确认 APP 下载、二维码、短信/邮件/链接通道合并入口可见。

## 当前验证命令

```bash
npm test -- --run
npm run lint
npm run build
```

## 保证界面一致的要求

- 使用 `npm ci`，不要用不同版本依赖覆盖 `package-lock.json`。
- 提交 `src/`、`public/`、`package.json`、`package-lock.json`、`packaging/`、`scripts/`。
- 不提交 `node_modules/`、`dist/`、`release/`。
- 克隆后用 `npm run demo` 打开 `http://127.0.0.1:5173/`。

## 下一阶段：核心单机可用版

- D1：继续扩大本机保存范围，让录音记录、面试档案、导入简历和更多评分数据可保存。
- D2：打通招聘主流程，让简历、录音、面试档案、三轮作业可以上传、查看、归档。
- D3：完善登录权限、基础导出、Mac/Windows 桌面启动包。
- D4：把真实 ASR、AI、短信、邮件、企微、招聘平台接口列入并行预留，不拖慢单机可用版。
